<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ladlb.directassemblee.synthesis">

    <application>

        <activity
            android:name=".SynthesisActivity"
            android:screenOrientation="portrait" />

        <provider
            android:name=".dependencies.DependenciesProvider"
            android:authorities="${applicationId}.synthesis.DependenciesProvider"
            android:directBootAware="true"
            android:exported="false"
            android:initOrder="980" />

    </application>

</manifest>
